# Compose Reduction Phase 5 — Provider Feature Report

Date: 2026-08-27

## Current status

Provider extraction is **structurally complete** through the automated gates.
The provider setup/edit/import/pairing UI, state, resources, tests, and route
registration now live in `:feature:provider`. The app remains the composition
root and supplies the settings-owned backup-preview renderer through the
feature request contract.

Runtime acceptance and paired incremental-performance gates remain open. The
idle-host release-like profile gate was rerun successfully on 2026-08-27, but
that is a packaging/profile check rather than a before/after timing result.
This does not close Phase 5 overall; playback stability/manual journeys and
later Phase 5 slices remain independent gates.

## Ownership and boundary evidence

- `:feature:provider` depends only on `:core:navigation`, `:core:ui`,
  `:domain`, and the ledgered temporary `:data` types.
- The provider boundary task rejects app imports, root navigation controllers,
  and `MainActivity`; its fixture coverage passes.
- No legacy provider package or old provider graph references remain in app or
  feature source.
- Provider-only `setup_*` and `stalker_*` resources were removed from app
  resources and copied with tracked locale translations into the feature.
- Generated baseline profiles were regenerated by the profile tool after the
  provider move; both generated files now contain feature-owned playback and
  provider symbols and zero legacy app-provider paths. They were not
  hand-edited.

## Automated checks

Passed:

- provider ViewModel and core progress-fraction unit tests (the feature owns
  one provider unit-test source file plus five boundary fixtures);
- the complete `:feature:provider:check` lifecycle (debug/release unit tests,
  lint, Kover verification, and the provider boundary guard);
- app route compatibility tests (`AppRouteCodecTest`, `ExternalDestinationTest`);
- provider instrumentation-test compilation;
- focused connected provider completion test (1/1), app navigation contracts
  (3/3), platform compatibility matrix (4/4), recording-disabled golden guard
  (1/1), and playback overlay tests (7/7) on `Television_1080p(AVD)`;
- the full app-connected suite completed 26 tests with 17 passed and 9 failed
  (one fewer app test than the historical 27-test run because the provider
  completion test now belongs to `:feature:provider`);
  the nine are the two known environment/fixture failures, three known
  PlayerSmoke failures, and four mixed-run timeout failures. The navigation
  class and all six premium golden methods passed in isolated reruns.
- provider lint and debug assembly;
- fresh `:app:assembleRelease` on 2026-08-27 (release lint/R8 completed with
  existing baseline/tooling warnings);
- app Kotlin compilation and debug assembly;
- core navigation/UI, playback, provider, and app navigation boundary tasks;
- provider runtime dependency report;
- `graphify update .` (14,445 nodes / 28,231 edges).

The app graph-boundary regression found during the 2026-08-27 focused rerun is
resolved: `FeatureGraphBoundaryTest` now includes the extracted provider graph
root, and the isolated plus combined unit reruns pass.

`:app:lintDebug` remains non-gating with 200 repository-wide errors (API-level,
Compose, translation, and unused-resource debt); no provider-specific lint
errors are present. The detailed automated evidence is in
`validation/phase5_provider/task8-automated-validation.md`.

## Open gates

- Connected provider instrumentation passed. Manual acceptance has progressed
  through source switching, D-pad focus, M3U file and backup document-chooser
  launchers, Drive account-picker handoff, QR pairing start/stop, a seeded
  existing-provider edit/cancel journey, and a sanitized cold-start route
  smoke check. No credentials or provider submission were performed. Full
  create/edit save, preview/import completion, cold-start URI import,
  second-device QR, and exhaustive focus-restoration journeys remain open. See
  `validation/phase5_provider/task9-runtime-validation.md`.
- Five paired compile/profile performance samples remain open because the
  provider extraction is not represented by a committed post-extraction ref;
  do not compare timings from the mixed dirty tree. See
  `validation/phase5_provider/performance-rerun.md`.
- Release-like/profile regeneration and stale-path verification passed on the
  idle host; `:app:assembleRelease` also passed after regeneration.
- Existing Phase 5 playback live-TV stability/manual/profile gates remain open
  under the playback report.
